<!DOCTYPE html>
<html lang="es">
<!-- INPORTING HEAD -->
<%- include("./partials/head.ejs") %>

<body>
    <!-- IMPORTING NAV -->
    <%- include("./partials/nav.ejs") %>
    <main>
        <div>
            <!-- here goes posts -->
            <% if(posts.length > 0) { %>
                <% posts.forEach( ( post ) => { %>
                     <a href="./blogBody"></a> <%= post.title %>   </a>
                    <p> <%= post.snippet %> </p>
             <%  }) %>
           <% } else { %>
            <p>No posts publishet yet.</p>
          <% } %>
        </div>

    </main>
    <!-- IMPORTING THE FOOTER -->
    <%- include("./partials/footer.ejs") %>
</body>

</html>